#include<unistd.h>
#include<stdio.h>
#include<string.h>
int sep(char str[1024],char argv[10][1024])
{
	int i,j=0;
	int argc=0;
	for(int i=0;i<strlen(str);++i)
	{
		if(str[i]=='\n')
		{
			break;
		}
		else if(str[i]==' ')
		{
			if(str[i+1]!=' ')
			{
				argv[argc][j]='\0';
				j=0;
				argc++;
			}
			continue;
		}

		else
		{
			argv[argc][j]=str[i];
			j++;
		}
	}
	argv[argc][j]='\0';
	return ++argc;
}
int main()
{
	char str[1024],argv[10][1024],cwd[1024];
	int argc,status;
	int i=0;
	fflush(stdin);
	fgets(str,1023,stdin);
	fflush(stdin);
	argc=sep(str,argv);
	printf("%s%s%s",argv[0],argv[1],argv[2]);
	// for(i=0;i<5;++i)
	// {
	// 	printf("%c",argv[3][i]);
	// }
	printf("%d",strcmp(argv[2],"test"));
	// printf("\n");
	return 0;
}